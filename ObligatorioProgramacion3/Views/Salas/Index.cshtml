@model IEnumerable<ObligatorioProgramacion3.Models.Sala>
@{
	ViewData["Title"] = "Lista de salas";
	Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row">
	<div class="col-4">
		<h3>Salas</h3>
	</div>
	<div class="col-3">
		<a asp-action="Crear" class="btn btn-primary form-control">Crear sala</a>
	</div>
</div>

<div>
	@if (Model.Count() > 0)
	{
		<table class="table table-striped border">
			<tr>
				<th>
					Salas:
				</th>
			</tr>
			@foreach (var sala in Model)
			{
				<tr>
					<td>@sala.Nombre</td>
					<td>
						<a class="btn btn-primary text-white" asp-action="Editar" asp-route-id="@sala.Id">Editar</a>
						<a class="btn btn-danger text-white" asp-action="Borrar" asp-route-id="@sala.Id">Borrar</a>
					</td>
				</tr>
			}
		</table>
	}
	else
	{
		<p>No hay salas</p>
	}
</div>

